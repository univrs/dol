// Test: Spirit-level visibility
// Expected: pub(spirit) items accessible within same Spirit only

pub gen Container {
    has id: u64
    has name: string
}

// Available within this Spirit only
pub(spirit) fun generate_id() -> u64 {
    return 42
}

pub(spirit) fun log_transition(id: u64, state: string) -> () {
    // internal logging
}

docs {
    Test case for Spirit-level visibility.
    pub(spirit) is equivalent to Rust's pub(crate).
    Items are accessible within the Spirit but not from other Spirits.
}
