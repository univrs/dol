// Test: Selective imports with braces
// Expected: use path.{ item1, item2 } imports specific items

use arithmetic.{ add, subtract, multiply, divide }
use container.state.{ ContainerState, is_active }

pub fun math_demo() -> i64 {
    return add(1, 2)
}

docs {
    Test case for selective imports.
    Braces allow importing specific items from a module.
    This keeps the namespace clean.
}
