gen CollaborativeList {
  @crdt(immutable)
  has list_id: String

  @crdt(rga)
  has items: Vec<String>
}

docs {
  A collaborative list using RGA (Replicated Growable Array).

  The list_id is immutable for identity.
  Items use RGA for causal ordering of insertions and deletions.
}
