gen InvalidExample {
  @crdt(or_set)
  has name: string

  @crdt(pn_counter)
  has description: string
}

docs {
  This example demonstrates invalid CRDT strategy usage.

  OR-Set cannot be used with string type (should use Set).
  PN-Counter cannot be used with string type (should use integer types).
  Running dol-check on this file will produce errors.
}
