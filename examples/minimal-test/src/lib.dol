// Minimal DOL module for testing

// Simple functions to test DOL -> Rust -> WASM pipeline
fun add(a: i32, b: i32) -> i32 {
    a + b
}

fun multiply(a: i32, b: i32) -> i32 {
    a * b
}

fun is_positive(n: i32) -> bool {
    n > 0
}

exegesis {
    Minimal DOL module to test compilation.
    Pure functions only - no complex types.
}
